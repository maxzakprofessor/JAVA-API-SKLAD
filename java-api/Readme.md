# Sklad Inventory Management System (Java Backend)

Современная реализация бэкенда для системы складского учета, мигрированная с стека ASP.NET на **Java 25 (LTS)** и **Spring Boot 3.5**.

## 🚀 Ключевые компетенции и технологии
*   **Backend:** Java 25, Spring Boot 3.5, Spring Data JPA.
*   **Database:** PostgreSQL (интеграция через Hibernate с автоматической миграцией схем).
*   **Architecture (SOLID):** 
    *   **S (Single Responsibility):** Четкое разделение на контроллеры (API), сервисы (Business Logic) и репозитории (Data Access).
    *   **D (Dependency Inversion):** Внедрение зависимостей через Spring IoC контейнер.
*   **Frontend Integration:** Полная стыковка с Vue.js через REST API, настройка CORS и кастомная десериализация JSON (Jackson).
*   **Build Tools:** Maven (Lifecycle management, dependency control).

## 🛠 Реализованная бизнес-логика
Реализован сложный алгоритм расчета остатков (**GoodRest**) в реальном времени. Алгоритм учитывает:
1. Прямые поступления товаров (`GoodIncome`).
2. Перемещения между складами в обе стороны (`GoodMove`).
3. Агрегацию данных в оперативной памяти (Java Streams) для высокой производительности с последующей атомарной записью в БД (`@Transactional`).

## 📦 Запуск проекта
1. Сборка: `./mvnw clean package -DskipTests`
2. Запуск: `java -jar target/java-api-0.0.1-SNAPSHOT.jar`
